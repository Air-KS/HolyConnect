// src/styles/scrollView.js

import {StyleSheet } from 'react-native';

const stylesScrollView = StyleSheet.create({
  scrollView: {
    backgroundColor: '#A6D2D2',
    flex: 1,
  },

});

export default stylesScrollView;
